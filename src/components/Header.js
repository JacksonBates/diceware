import React from 'react';

const Header = () => {
  return <div className="hero hero-sm bg-dark">
    <h1>Diceware Passphrase Generator</h1>
    <p>A simple diceware implementation using a cryptographically secure random number generation and the EFF wordlist.</p>
  </div>
}

export default Header;